

const Header = (props) => {
    return (
        <header>
       <button>Add</button>
      
        </header>
    )
}

export default Header
/*const Header = ({title}) => {
    return (
        <header>
          <h1 style={{color:'red'}} >{title}</h1>
          <button>Add</button>
        </header>
    )
}

export default Header*/
